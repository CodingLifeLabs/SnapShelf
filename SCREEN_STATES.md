# SnapShelf — Screen States

> 각 표면의 상태 기계(정상/빈/로딩/에러/권한). 회귀 테스트는 이 상태별로 캡처 비교.

## 1. Menu Bar Status Item

| 상태 | 아이콘 | 조건 |
|------|--------|------|
| idle | 단색 로고(ink) | 감시 중, 새 항목 없음 |
| active | accent 링 + 카운트 배지 | 새 항목 1+ |
| paused | 점선 회색 | 감시 일시정지(권한/설정) |
| error | warm 점 | 오류(권한거부/디스크) — 툴팁 |

## 2. ShelfPanel

```
[empty]      권한 OK, 항목 0
   → "⌘⇧4 로 캡처하면 여기에 쌓입니다" 일러스트 + 힌트

[intake]     캡처 직후
   → 우하단→스택 빨려듦 애니메이션 + accent-warm 링

[resting]    항목 N(≤히스토리한도)
   → 스택 + 상단 검색입력 + 호버 툴바

[searching]  검색어 입력
   → 디바운스 후 로딩 스켈레톤 → 결과 스니펫 하이라이트

[no-results] 검색 결과 0
   → " '{query}' 에 대한 결과 없음" + OCR 재시도/AI 검색 제안

[pinned]     핀 항목 존재
   → 상단 고정 섹션(`.title3` 헤더 "Pinned")

[permission] 권한 부족
   → "스크린샷 폴더 접근 필요" 카드 + "설정 열기/폴더 선택"

[error]      로드 실패
   → 에러 카드 + 재시도
```

## 3. Library Window

```
[empty]      라이브러리 0 / 색인 미완료
   → 온보딩 카드 + "지금 색인"

[loading]    색인/이동 진행
   → 진행률(항목 수) + 취소

[browsing]   폴더/컬렉션 선택
   → 사이드바(선택 강조) + 그리드/리스트 + 선택 디테일

[organizing] Smart Folder 실행 중
   → 사이드바 폴더에 카운트 변화 + 마이크로 애니메이션
```

## 4. Timeline

```
[empty]   타임라인 데이터 없음
[loaded]  시간축 + 항목 그룹(앱 색 점)
[scrub]   스크럽 중 → 해당 시점 그룹 하이라이트 + 상세 카드
```

## 5. Settings

```
[ai-off]      AI 미옵트인 → provider 섹션 비활성(점선) + "AI 켜기"
[ai-on]       provider 선택 → 모델/키(Keychain) 입력 → 연결 테스트 버튼
[indexing]    재색인 진행률
[reset]       "모든 데이터 초기화" 2단계 확인(위험 동작 → 사용자 확인 필수)
```

## 6. 공통 상태 규칙

- **reduce-motion ON**: 모든 애니메이션 → 즉시/페이드 전환. 빈/에러 상태는 정적 일러스트.
- **다크 모드**: 유리 비브랜시 강조, 악센트 동일 유지.
- 모든 에러/권한 상태는 **복구 경로(버튼)** 를 반드시 포함.
